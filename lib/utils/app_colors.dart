import 'package:flutter/material.dart';

class AppColors {
  // Dark theme - Cool professional colors
  static const primary = Color(0xFF00D9FF); // Cyan blue
  static const primaryLight = Color(0xFF4DE8FF);
  static const primaryDark = Color(0xFF00A8CC);
  
  // Dark backgrounds
  static const background = Color(0xFF0A0E27); // Deep navy
  static const surface = Color(0xFF151932); // Dark blue-gray
  static const surfaceLight = Color(0xFF1E2139); // Lighter surface
  
  // Text colors for dark theme
  static const textPrimary = Color(0xFFE8EAF6); // Light blue-white
  static const textSecondary = Color(0xFF9FA8DA); // Muted blue-gray
  
  // Status colors (vibrant for dark mode)
  static const success = Color(0xFF00E676); // Bright green
  static const warning = Color(0xFFFFAB00); // Amber
  static const error = Color(0xFFFF5252); // Bright red
  static const disabled = Color(0xFF424242); // Dark gray
  
  // Accent colors
  static const accent = Color(0xFF7C4DFF); // Purple
  static const accentSecondary = Color(0xFFFF6E40); // Orange
}
